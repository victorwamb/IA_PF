// Loader.jsx
import React from "react";
import "./loader.css"; // Importez le fichier CSS qui contient le code du loader

const Loader = () => {
  return (
    <div className="center">
      <div className="loader"></div>
    </div>
  );
};

export default Loader;
